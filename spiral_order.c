#include<stdio.h>
int main()
{
    // row0 หลักfinal rowสุดท้าย หลัก0 ถึง row(i+1)
    int m, n;
    int i, j;
    i = 0;
    j = 0;
    scanf("%d %d", &n, &m);
    int total[m*n];
    int matrix[m][n];


    for (int i = 0; i < )
    while (i != n)
    {
        total[i] = matrix[0][j];
        i++;
    }
    
    while (i != m)
    {
        total[i] = matrix[n-1][j+1];
    }
}